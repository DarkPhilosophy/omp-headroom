---
name: Bug report
about: Something broke — compression, compaction, widget, installer
labels: bug
---

**What happened**

**Expected behavior**

**Environment**
- OMP version / fork (right-panel fork or stock):
- `headroom --version`:
- GPU backend (nvidia / amd-rocm / cpu):
- Proxy managed by (systemd unit / extension-spawned):

**Payload shape (never content)**
- Provider format (messages / anthropic / responses):
- Item types + approximate sizes:

**Logs**
`~/.headroom/logs/proxy.log` excerpts (strip any payload text), widget error text, or `OMP_HEADROOM_DEBUG=1` shape lines.
